[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Wrap in bash -c to expand $PORT properly
startCommand = "/bin/bash -c 'bin/rails server -b 0.0.0.0 -p $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
